for ii=1:100
    Tester
end
% 
% k=[4,5,3,7,9,8,3];
% if length(unique(k))<length(k)
%     u=unique(k,'first')
%     num=length(k)-length(u);
%     u(length(u),
% end

% [r,c] = find(kclusters==min(kclusters)) ;   % Get row and column indices.
%  for k = 1 : size(seedMeans,1)
%      kclusters(r(k), c(k)) = 0 ;
%  end